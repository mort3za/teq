{
  "name": "x-teq",
  "version": "1.0.0",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun run build.ts",
    "watch": "bun run build.ts --watch",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/chrome": "^0.1.43"
  },
  "peerDependencies": {
    "typescript": "^6.0.3"
  }
}
